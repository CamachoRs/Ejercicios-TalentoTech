import React from 'react';

const LazyComponent = () => {
    return (
        <div>
            <h2>¡Este es un componente cargado de forma diferida!</h2>
        </div>
    );
};

export default LazyComponent;